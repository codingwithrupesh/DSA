import java.util.HashMap;

class ThirdMaxElement {
    public static void main(String[] args) {
        int[] arr = {11, 13, 11, 15, 14, 11};
//        thirdMax(arr);
    }

//    public static <TreeSet> void thirdMax(int[] arr) {
//        // Using a TreeSet to keep unique elements in sorted order
//        TreeSet<Integer> uniqueElements = new TreeSet<>();
//        for (int num : arr) {
//            uniqueElements.add(num);
//        }
//
//        // Check if there are at least 3 unique elements
//        if (uniqueElements.size() < 3) {
//            System.out.println("There are not enough unique elements to determine the third maximum.");
//        } else {
//            // Retrieve the third maximum element
//            Iterator<Integer> iterator = uniqueElements.descendingIterator();
//            iterator.next(); // Skip the maximum element
//            iterator.next(); // Skip the second maximum element
//            int thirdMax = iterator.next(); // Get the third maximum element
//            System.out.println("The third maximum element is: " + thirdMax);
//        }
//    }

}